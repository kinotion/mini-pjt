# SERVICE · 키즈밀플래너 Agent

> 아이의 나이·알레르기·형제자매 구성에 맞춰 안전하고 근거 있는 식단을 추천하는 AI 에이전트

---

## 0. 구현 범위

이 프로젝트는 **v1 범위만 구현**하고, 나머지는 설계 문서로만 남긴다.

| 구분 | 내용 |
|---|---|
| **v1 (구현)** | 단일 자녀 · 나이/알레르기 안전 필터 · RAG 근거 제시 · HITL(승인) · **장기 기억(자녀 프로필 + 냉장고 재료)** · **3일치(9끼)** 식단표 |
| **확장 (문서만 — 2장)** | 다자녀 대응, 제철 재료 가산점, 유통기한 가중치 기반 추천 점수 스택 |

v1을 좁게 잡는 이유: 레시피·지식 문서 규모가 작아 가산점 로직을 여러 겹 쌓으면 필터를 통과한 후보가 남지 않고, 정작 평가 대상인 안전성·근거성·승인 흐름이 부실해진다. 확장 항목은 2장에 설계만 정리해 두고 구현하지 않는다.

**에이전트 패턴 커버리지**

| 패턴 | v1에서 구현되는 위치 |
|---|---|
| **ReAct** | `create_agent` 기반 추론·행동 루프. 고정 파이프라인이 아니라, 도구 결과에 따라 **다음 행동을 다시 정한다** (아래 "ReAct 루프가 실제로 돌아야 하는 지점" 참고) |
| **RAG** | `search_nutrition_guidelines` + `src/retriever.py` → 응답의 `contexts` 키 |
| **도구 다중** | v1 도구 8개 (3장) |
| **HITL** | `save_meal_plan_to_file`, `override_preference_constraint` → `interrupt` 기반 `y/n` 승인 (4장) |
| **장기 메모리** | `data/memory_store.json` 영속 저장 + `get_household_memory` / `update_household_memory` (세션이 끝나도 자녀 프로필·냉장고 재료 유지) |
| **평가** | `evaluation/test_queries.csv` + 패턴별 질의 카테고리 + 측정 방법이 붙은 기준 (6장) |

**ReAct 루프가 실제로 돌아야 하는 지점**

"검색 → 검증 → 표 생성"을 한 줄로 흘려보내면 도구를 순서대로 호출한 것일 뿐 ReAct가 아니다. v1은 아래 **재계획(re-plan) 분기**를 반드시 포함한다.

1. `check_allergen`에서 후보가 탈락하면 → 해당 재료를 제외 조건에 넣고 `search_recipe`를 **다시** 호출한다
2. `search_recipe` 결과가 비면 → 나이 범위나 영양소 포커스 조건을 완화해 재검색한다
3. 냉장고에 재료가 없으면 → 보유 재료 우선 활용을 포기하고 일반 검색으로 넘어간다
4. 영양 근거 질문이 섞여 들어오면 → 식단 생성 전에 `search_nutrition_guidelines`를 먼저 호출한다

`trace`에는 이 판단 과정이 호출 순서대로 남아야 하며, 평가 시 **재탐색이 일어난 질의에서 `search_recipe`가 2회 이상 호출됐는지**로 루프 동작을 확인한다.

---

## 1. 사용자·문제·가치

**타깃 사용자**
- 영유아~초등 저학년 자녀를 둔 보호자 (특히 자녀가 2명 이상인 가정)
- 매일 "오늘 뭐 해줘야 하지"를 반복적으로 고민하는 사람

**핵심 문제**
- 끼니마다 메뉴를 새로 고민해야 하는 반복 피로
- 알레르기가 있는 아이에게 위험한 재료가 들어가지 않도록 매번 확인해야 하는 부담
- "이 영양소가 왜 필요한지" 근거 있는 정보를 찾기 어렵고, 잘못된 정보에 의존할 위험
- 자녀가 2명 이상일 때, 나이·알레르기가 서로 달라 식단이 이중으로 복잡해짐 (조리 부담 증가)
- 냉장고에 있는 재료를 활용 못 하고 매번 새로 장을 봐서 **식재료를 낭비**하거나, 유통기한을 넘겨 버리는 재료가 생김
- 제철이 아닌 재료 위주로 장을 봐서 **더 비싸고 영양가 낮은 식재료**를 아이에게 먹이게 되는 경우가 많음

**제공 가치**
- 나이·알레르기 조건에 맞는 **안전한** 메뉴를 자동으로 필터링
- "왜 이 메뉴/영양소가 필요한지"를 지식 문서 기반으로 **근거 있게** 설명 (RAG)
- 형제자매의 조건을 함께 고려해 **공통 메뉴는 하나로, 다른 부분만 대체**하는 실용적 식단표 제공
- 위험한 작업(파일 저장, 알레르기 경고 우회)은 보호자의 명시적 승인 없이는 실행되지 않음 (HITL)
- **장기 기억에 저장된 자녀 프로필과 냉장고 재료 정보**를 활용해, 매번 조건을 다시 말하지 않아도 되고 이미 집에 있는 재료를 우선 소진하는 식단을 짜서 **식비 절감과 음식물 낭비 감소**에 기여
- *(확장 설계)* **오늘 날짜 기준 한국의 제철 재료**를 우선 반영해, 더 신선하고 저렴하며 영양가 높은 식단을 자연스럽게 유도

---

## 2. 서비스 확장 관점 (설계만 — v1 구현 범위 밖)

> 다자녀와 제철 가산점은 **구현하지 않는다.** 서비스가 커질 때 어떤 순서로 무엇을 건드려야 하는지를 미리 정리해 둔 것이다.
> 단 **장기 기억은 예외다** — 저장소와 조회/갱신 도구는 v1에서 구현하고, 유통기한 가중치를 매기는 추천 점수 스택만 확장으로 남긴다. 아래 장기 기억 절에 어디까지가 v1인지 표시했다.

**단일 자녀 → 다자녀 확장 원칙**

| 항목 | 단일 자녀 (현재 구현) | 다자녀 확장 시 |
|---|---|---|
| 데이터 모델 | 나이 1개, 알레르기 1세트 | `children: [{name, age, allergens}, ...]` 리스트 구조 |
| 메뉴 구성 방식 | 필터링만 하면 됨 | **통합형(기본값)**: 공통 메뉴 우선, 안 되는 부분만 아이별 대체 메뉴 추가 / **분리형(옵션)**: 아이별 완전 독립 식단 |
| 검색 로직 | 나이 1개로 검색 | 두 아이 나이 **교집합 범위**로 먼저 검색 → 교집합이 비면(이유식 vs 초등처럼 나이 차가 큰 경우는 거의 항상 빈다) `base_ingredient`가 같은 메뉴를 찾고, 그것도 없으면 각자 검색 |
| 알레르기 판정 | 아이 1명 기준 안전 여부 | 아이별 개별 판정 후, **한 명이라도 걸리면** 해당 메뉴는 "공통 불가"로 처리하고 대체 메뉴 배정 |
| 나이 차이가 큰 경우 (이유식 vs 일반식) | 해당 없음 | 같은 재료를 아이별로 다른 조리법으로 매칭 (예: 소고기 채소죽 ↔ 소고기 채소볶음) — 레시피에 `base_ingredient` 태그 추가 필요 |
| 출력 표 | 요일 · 끼니 · 메뉴 · 비고 | 요일 · 끼니 · 공통 메뉴 · 첫째만 · 둘째만 · 비고 |

**로드맵 우선순위**
1. `children` 리스트 데이터 모델 도입
2. 재료 기반 조리법 매칭 (`base_ingredient` 태그) — 나이 교집합이 비는 조합에서 "공통 재료"를 잡아주는 **전제 조건**이므로 통합형 로직보다 먼저 온다
3. 통합형 식단 생성 로직 (공통 재료 → 공통 메뉴 순으로 탐색)
4. 완전 분리형 옵션 지원

---

**장기 기억(Long-term Memory)** — *저장소·도구는 v1 구현, 유통기한 가중치 스택만 확장*

지금까지의 도구는 모두 **한 번의 대화(세션) 안에서만** 정보를 주고받았습니다.
하지만 "냉장고에 뭐가 있는지"는 대화가 끝나도 유지되어야 하는 정보이므로,
대화 히스토리와 별도의 **영속 저장소(persistent store)**가 필요합니다.

| 구분 | 대화 메모리 (기존) | 장기 기억 (신규) |
|---|---|---|
| 유지 범위 | 한 세션(대화) 안에서만 | 세션이 끝나도 계속 유지 |
| 저장 위치 | `messages` 리스트 (메모리상) | 로컬 파일 `data/memory_store.json` (규모가 커지면 SQLite) |
| 갱신 시점 | 매 턴마다 자동 누적 | 사용자가 "우유 샀어", "당근 다 썼어", "둘째 6살 됐어"라고 말할 때 **명시적으로만** 갱신 |
| 용도 | 이전 대화 맥락 참고 | 자녀 조건 재입력 없이 **알레르기 안전 판정**, 식단 생성 시 **보유 재료 우선 활용** |

**장기 기억 데이터 모델 (v1)**

자녀 프로필도 세션이 끝나면 사라지면 안 되는 정보다. 매번 "우리 애 5살이고 달걀 알레르기 있어"를 다시 말하게 하지 않으려면 재료와 같은 저장소에 둔다. 알레르기 정보가 영속화되면 안전 판정이 **기억에서 바로** 출발한다는 점에서도 의미가 크다.

```json
{
  "household_id": "family_001",
  "children": [
    {"name": "첫째", "age_months": 62, "allergens": ["달걀", "우유"], "updated_at": "2026-09-16"}
  ],
  "ingredients": [
    {"name": "두부", "quantity": "1모", "expiry_date": "2026-09-20", "updated_at": "2026-09-16"},
    {"name": "당근", "quantity": "2개", "expiry_date": "2026-09-25", "updated_at": "2026-09-16"}
  ]
}
```

v1은 `children` 배열의 **원소 1개만** 사용한다. 리스트 구조로 잡아두는 이유는 다자녀 확장 때 스키마를 갈아엎지 않기 위해서다.

**식단 생성 흐름에 통합되는 방식**
1. **(v1)** 식단 요청이 들어오면 먼저 `get_household_memory`로 자녀 프로필과 보유 재료를 불러온다
2. **(v1)** `search_recipe`에 보유 재료를 넘겨, 보유 재료를 쓰는 레시피를 **단순 우선 정렬**한다 (점수 가중치 없이 "쓰는가 / 안 쓰는가" 이분 판정). 단 **유통기한이 지난 재료는 제외**한다
3. **(v1)** 최종 식단표에는 "냉장고에 있는 재료로 만들 수 있어요" 같은 표시를 추가한다
4. **(v1)** 사용자가 장을 보거나 재료를 다 쓰면 `update_household_memory`로 재고를 갱신한다 (실제 냉장고 센서 연동은 후속 확장 과제)
5. **(확장)** 유통기한 임박 정도를 가중치로 환산해 제철 점수와 함께 순위를 매기는 점수 스택 — 레시피 DB가 충분히 커진 뒤에 도입한다

---

**제철 재료 가산점 로직**

한국은 계절에 따라 맛과 영양, 가격이 크게 달라지는 재료가 많습니다(봄 딸기·냉이, 여름 애호박·수박, 가을 밤·연근, 겨울 배추·귤 등). 오늘 날짜를 기준으로 **지금이 제철인 재료를 쓰는 메뉴에 가산점**을 줘서, 신선하고 저렴한 식재료가 자연스럽게 더 자주 추천되도록 합니다.

**제철 데이터 모델 (초안)**
```python
SEASONAL_INGREDIENTS = {
    "딸기": {"months": [12, 1, 2, 3, 4]},
    "냉이": {"months": [2, 3, 4]},
    "애호박": {"months": [6, 7, 8]},
    "수박": {"months": [6, 7, 8]},
    "밤": {"months": [9, 10, 11]},
    "연근": {"months": [10, 11, 12]},
    "배추": {"months": [11, 12, 1]},
    "귤": {"months": [11, 12, 1, 2]},
    # ... 계속 확장
}
```

**동작 방식**
1. 오늘 날짜(월)는 `get_seasonal_ingredients` 도구가 **내부에서 직접 구한다**(`date.today()`). 모델은 오늘 날짜를 모르므로 날짜를 인자로 받게 설계하면 엉뚱한 계절이 나온다
2. 각 레시피의 재료 중 제철 재료가 포함되어 있으면 점수를 가산한다 (예: 제철 재료 1개당 +1점)
3. 냉장고 가산점과 제철 가산점은 **단순 합산하지 않는다.** 합산하면 제철 재료 3개짜리가 냉장고 재료 1개짜리를 이겨서 아래 우선순위 스택이 깨진다. 정렬 키를 `(냉장고 점수, 제철 점수, 다양성 점수)` 튜플로 두고 **사전식(lexicographic)으로 비교**한다
4. 최종 식단표에는 "지금 제철인 OO(으)로 만든 메뉴예요" 같은 안내 문구를 붙여 사용자가 이유를 알 수 있게 한다

**여러 가산점/제약이 겹칠 때의 우선순위 스택** (위에서 아래로 절대 우선)
1. **나이 적합성 · 알레르기 안전성** — 필수 조건, 통과 못 하면 후보에서 완전 제외
2. **냉장고 보유 재료 활용** — 특히 유통기한 임박 재료는 가산점 가중치를 더 높게
3. **제철 재료 활용** — 위 두 조건을 만족한 후보들 사이에서 우선순위를 매기는 보조 가산점
4. **메뉴 다양성(중복 최소화)** — 최종적으로 같은 주간 안에 메뉴가 겹치지 않도록 조정

즉 제철 재료는 어디까지나 **동점자를 가르는 보조 기준**이며, 안전성이나 나이 적합성을 침해하지 않습니다.

---

## 3. 사용 예상 도구·데이터

**v1 도구 (구현 대상)**

| 도구명 | 역할 | 위험도 |
|---|---|---|
| `search_recipe` | 나이·끼니(·영양소 포커스)로 레시피 후보를 구조화 검색 | 일반 |
| `check_allergen` | 레시피의 **재료를 구성 성분까지 펼쳐**, 사용자가 정의한 알레르기 프로필과 충돌하는지 확인 | 일반 |
| `search_nutrition_guidelines` | 영양 지식 문서에서 관련 근거를 검색 (RAG, TF-IDF 기반) | 일반 |
| `get_household_memory` | 장기 기억에서 자녀 나이·선호와 냉장고 재료 목록을 조회 (알레르기 프로필은 기동 시 로딩되어 항상 적용) | 일반 |
| `get_allergy_profile` | 알레르기 프로필 내용(무엇에·얼마나 심하게)을 조회. 사용자가 프로필 자체를 물어볼 때 사용 - **읽기 전용**, 대응하는 쓰기 도구 없음 | 일반 |
| `update_household_memory` | 자녀 나이·선호 변경, 재료 구매/소진을 장기 기억에 반영. **알레르기는 건드리지 않는다** | 일반 (변경 이력 로그 권장) |
| `generate_meal_plan_table` | 확정된 메뉴를 최종 식단표(마크다운)로 정리 | 일반 |
| `save_meal_plan_to_file` | 식단표를 로컬 파일로 저장 | **위험 (HITL 승인 필요)** |
| `override_preference_constraint` | 알레르기가 **아닌** 제약(편식·선호·종교/비건 등)을 사용자 요청으로 해제 | **위험 (HITL 승인 필요)** |

> 초안의 `override_allergen_conflict`(알레르기 충돌 메뉴 강제 포함)는 **삭제했다.** 정책 1의 "알레르기 무관용"과 정면으로 충돌하고, 아동 알레르기는 승인 한 번으로 넘길 성격의 위험이 아니다. HITL 시연은 파일 저장 + 선호 제약 해제 두 가지로 충분하다.

**확장 도구 (설계만 — 구현하지 않음)**

| 도구명 | 역할 |
|---|---|
| `get_seasonal_ingredients` | 오늘 날짜 기준 제철 한국 식재료 목록 조회 |

**데이터**

경로는 CLAUDE.md 폴더 규약을 따른다. 데이터는 `data/`에 JSON으로 두고, 로더는 `src/tools.py`·`src/retriever.py` 안에 둔다 (규약에 없는 `.py` 데이터 모듈을 새로 만들지 않기 위함).

| 데이터 | 내용 | 파일 | 범위 |
|---|---|---|---|
| 레시피 DB | 메뉴명, 추천 나이대, **재료 목록**, 영양소 태그 | `data/recipes.json` | v1 |
| 재료 구성 사전 | 재료 → 들어 있는 성분. 간장→대두·밀, 마요네즈→달걀·대두 같은 **숨은 성분** 포함 | `data/allergen_map.json` | v1 |
| **알레르기 프로필** | **사용자가 직접 작성** — 우리 아이가 피해야 할 것. 판정의 유일한 원천 | `data/allergy_profile.json` | v1 |
| 영양 지식 문서 | 이유식 시기, 알레르기 도입 가이드, 철분/칼슘/오메가3, 편식 대처법, 간식 원칙 등 (현재 8개) | `data/knowledge/*.md` | v1 |
| **장기 기억 저장소** | 자녀 나이·선호(v1은 1명) + 냉장고 재료(재료명·수량·유통기한·갱신일) — **세션 간 영속 저장**. 알레르기는 여기 두지 않는다 | `data/memory_store.json` | v1 |
| 제철 재료 캘린더 | 재료명 → 제철 월(month) 매핑, 한국 기준 | `data/seasonal_ingredients.json` | 확장 |

**알레르기 판정은 태그가 아니라 재료 단위로 한다**
레시피에 수기로 붙인 알레르기 태그만 보면 간장 속 밀, 마요네즈 속 달걀 같은 숨은 알레르겐을 놓친다. "알레르기 충돌 0건"이라는 성공 기준은 태그 매칭으로는 담보되지 않으므로, `check_allergen`은 **레시피 재료를 알레르겐 매핑으로 펼친 뒤** 판정한다.

**데이터 규모**

레시피 10개에 나이 필터 + 알레르기 필터를 걸면 후보가 0~1개로 떨어지고, 일주일(21끼) 식단에서 "메뉴 중복 최소화"는 산술적으로 불가능하다.
→ v1은 **레시피 40개 이상 / 산출물은 3일치(9끼)** 를 기준으로 한다. 일주일 식단표는 레시피 DB가 충분히 커진 뒤에 연다.

---

## 4. 인터페이스와 승인 흐름 (제출 규약 연계)

CLAUDE.md 규약은 `POST /query`로 `question` 하나를 받고 `answer / contexts / trace` 를 돌려주는 **단발 요청·응답**이다. 한 번의 응답 안에서 `y/n`을 주고받을 수 없으므로 아래를 확정해 둔다.

**응답 키 정의**

| 키 | 담는 것 |
|---|---|
| `answer` | 사용자에게 보여줄 최종 텍스트(식단표 마크다운 포함). 승인 대기 중이면 승인을 요청하는 문구 |
| `contexts` | `search_nutrition_guidelines`가 실제로 인용한 **지식 문서 청크만**. 레시피 검색 결과는 넣지 않는다 (근거성 지표가 오염된다) |
| `trace` | 호출된 도구 이름·인자·결과 요약의 순서 리스트 + 승인 상태(`status: ok` 또는 `pending_approval`) |

**HITL 승인 흐름**

1. 위험 도구 호출 직전에 LangGraph `interrupt`로 그래프를 멈춘다
2. `answer`에 승인 요청 문구, `trace.status`에 `pending_approval`, `trace.pending`에 대기 중인 도구명과 인자를 담아 응답한다
3. 다음 요청의 `question`이 `y`/`n`이면 같은 스레드를 재개해 도구를 실행하거나 취소한다

**세션 식별자**

규약에 스레드 필드가 없으므로 v1은 단일 가정을 가정하고 `thread_id`와 `household_id`를 고정값(`"default"` / `"family_001"`)으로 둔다. 이 고정 키 덕분에 **대화 스레드가 끊겨도 장기 기억은 같은 가정 레코드를 계속 가리킨다** — 세션 메모리와 장기 메모리를 가르는 지점이 여기다. 다중 사용자 지원은 확장 과제이며, 그때는 요청 헤더나 필드 추가가 함께 필요하다.

---

## 5. 서비스 정책 (가드레일 요약)

**v1 정책**

1. **알레르기 무관용 원칙**: `check_allergen`을 통과하지 못한 메뉴는 식단에 절대 포함하지 않는다. 판정 기준은 일반 규제 목록이 아니라 **사용자가 `allergy_profile.json`에 정의한 우리 아이의 알레르기**이며, 재료를 구성 성분까지 펼쳐서 대조한다. `severity`가 낮아도 필터링 강도는 동일하다.
2. **위험 작업은 사람이 최종 승인**: 파일 저장과 **선호 제약 해제**는 시스템이 자동 실행하지 않고 반드시 보호자의 `y/n` 승인을 거친다 (HITL). **알레르기 안전 검증은 승인 대상이 아니다 — 우회 수단 자체를 제공하지 않는다.**
3. **영양 조언은 근거 기반으로만 제공**: 일반적인 영양 지식 질문에는 `search_nutrition_guidelines`(RAG)로 검색한 문서 내용에 근거해서만 답하고, 지어내지 않는다 (환각 방지). 검색 결과가 비면 "관련 문서를 찾지 못했다"고 답한다.
4. **전문 상담 대체 불가 고지**: 모든 답변 말미에 "이 식단은 참고용이며, 정확한 영양 상담은 소아과 전문의나 영양사와 상의하세요"라는 문구를 포함한다.

5. **기억된 재료는 참고용 우선순위일 뿐, 안전 원칙에 우선하지 않는다**: 보유 재료를 우선 소진하도록 추천하되, 알레르기 무관용 원칙(정책 1)이 항상 우선한다. 즉, 냉장고에 있어도 알레르기 재료면 추천하지 않는다. 유통기한이 지난 재료는 추천 근거로 쓰지 않는다.
6. **장기 기억은 개인정보로 취급**: 자녀 프로필과 재료 목록은 해당 가정 전용 저장소에만 보관하고 다른 사용자와 공유하지 않는다. 시스템이 기억을 임의로 지우지 않는다 — 만료 재료는 "폐기 권장" 안내만 하고, 기록 삭제는 사용자 확인 후 진행한다.
7. **기억은 명시적으로만 갱신한다**: `update_household_memory`는 사용자가 "우유 샀어", "둘째 이제 6살이야"처럼 알려줬을 때만 호출한다. 에이전트가 대화 내용을 추측해 몰래 기억을 바꾸지 않는다.
8. **알레르기 프로필은 에이전트가 쓰지 않는다**: `allergy_profile.json`은 **읽기 전용**이다. 대화 중 "이제 달걀 괜찮아진 것 같아" 같은 말이 나와도 파일을 고치지 않고, 사용자가 직접 수정하도록 안내한다. 오인식 한 번으로 알레르기 항목이 사라지는 것이 이 서비스에서 가능한 최악의 사고다.

**확장 정책 (설계만 — 구현하지 않음)**

9. **다자녀 처리 기본 정책**: 기본값은 통합형(공통 메뉴 우선)으로 하되, 조리 부담을 고려해 완전 분리형은 명시적 요청 시에만 제공한다. 다자녀 환경에서는 한 명이라도 알레르기 충돌이 나면 공통 메뉴에서 제외한다. 아이별 알레르기 프로필은 `allergy_profile.children[]`에 각각 추가한다.
10. **제철 재료 가산점은 보조 기준일 뿐, 필수 조건에 우선하지 않는다**: 나이 적합성·알레르기 안전성 → 냉장고 재료 활용 → 제철 재료 순으로 우선순위가 적용되며, 제철이 아니라는 이유로 안전하고 적합한 메뉴가 제외되는 일은 없다.

---

## 6. 성공 기준

모든 기준은 `evaluation/test_queries.csv`의 질의 세트로 잰다. 측정 방법이 없는 항목은 기준으로 두지 않는다.

`test_queries.csv`는 **패턴별 카테고리 컬럼**(`pattern`)을 두어 어떤 질의가 어떤 패턴을 검증하는지 표시한다 — `react` / `rag` / `multi_tool` / `hitl` / `memory` / `safety`. 이러면 평가 리포트가 그대로 패턴 커버리지 증빙이 된다.
장기 메모리와 HITL은 **단발 질의로 검증할 수 없으므로**, CSV에 `turn` 컬럼을 두고 같은 `case_id`의 여러 행을 순서대로 실행하는 다중 턴 시나리오를 지원한다 (예: 1턴 "우유 샀어" → 2턴 "냉장고에 뭐 있어?").

**v1 평가 기준**

| 구분 | 기준 | 측정 방법 |
|---|---|---|
| **안전성** | 알레르기 충돌 메뉴가 식단표에 포함되는 사례 **0건** | 알레르기 보유 아동 질의 세트 → 출력 식단의 전 재료를 알레르겐 매핑으로 펼쳐 자동 검사 |
| **근거성** | 영양 질문 답변이 `contexts`의 문서 내용에 근거한 비율 | `contexts` 비어있지 않음(자동) + 답변 핵심 주장이 해당 청크에 있는지(수동 채점) |
| **승인 흐름** | 위험 도구 요청 시 100% 승인 절차를 거치는지 | 위험 질의 세트 → 1차 응답의 `trace.status == pending_approval` 확인 (자동) |
| **ReAct 루프** | 도구 결과에 따라 재계획이 일어나는지 | 알레르기로 후보가 탈락하는 질의에서 `trace`상 `search_recipe` 호출이 **2회 이상**인지 (자동) |
| **도구 선택** | 질문 유형에 맞는 도구를 고르는지 | 순수 영양 지식 질문에 `search_recipe`를 호출하지 않는지 등, 질의별 기대 도구 집합과 `trace` 비교 (자동) |
| **기억 지속성** | 프로세스를 재시작해도 기억이 유지되는지 | 다중 턴 시나리오: 1턴에 `update_household_memory` → **앱 재시작** → 2턴 조회 결과가 일치하는지 (자동) |
| **고지 문구** | 모든 답변에 전문 상담 고지 포함 | 문자열 포함 검사 (자동) |
| **응답 다양성** | 3일치(9끼) 식단 내 메뉴 중복 최소화 | 고유 메뉴 수 ÷ 9 |
| **실용성(체감)** | "이 식단표대로 장을 보고 요리할 수 있겠다"고 느끼는지 | 정성 평가 — 평가 리포트에 서술 |

**확장 기능 평가 기준 (구현 시 적용)**

낭비 감소(유통기한 임박 재료 우선 반영률)·제철 활용률·가산점 우선순위 일관성·다자녀 대응력.

참고로 **재료 활용률**(식단표에서 보유 재료를 실제로 쓴 메뉴 비율)은 v1에서도 잴 수 있으므로, 여유가 되면 v1 표에 추가한다.

### 6.1 `test_queries.csv` 컬럼 규격

7개 컬럼 고정. 여러 값은 `;`로 구분한다. `forbidden`·`expected_tools`·`note`는 선택(빈 칸 가능), 나머지 4개는 필수다. **단발 질의 20건**으로 구성하며, 다중 턴(승인 y/n 왕복, 재시작 후 기억 확인)은 이 파일로 표현하지 않고 `input` 텍스트 안에 전제를 녹여 단일 턴으로 검증한다 — 이 방식의 한계는 6.2에 정리했다.

| 컬럼 | 필수 | 값 | 설명 |
|---|---|---|---|
| `id` | ✅ | int | 1부터 순번 |
| `category` | ✅ | enum | `positive` / `negative` / `edge` / `guardrail` |
| `input` | ✅ | string | `POST /query`의 `question` 값 |
| `expected_traits` | ✅ | string[] | 답변에 반드시 있어야 할 항목(톤·구조·필수 문구) |
| `forbidden` | 선택 | string[] | 답변에 있으면 안 되는 항목 |
| `expected_tools` | 선택 | tool[] | `trace`에 반드시 있어야 하는 도구. 빈 칸이면 도구 호출 여부를 채점하지 않는다 |
| `note` | 선택 | string | 이 케이스가 무엇을 잡으려는지 |

**카테고리 구성 (20건: positive 8 · negative 4 · edge 5 · guardrail 3)**

| 카테고리 | 의미 | 이 프로젝트에서의 예 |
|---|---|---|
| `positive` (40%) | happy path — 이런 질문엔 이런 답이 나와야 한다 | 나이 맞춤 추천, RAG 인용, 냉장고 재료 활용, 식단표 생성, HITL 승인 요청 |
| `negative` (20%) | 이런 답은 나오면 안 된다 / 없는 정보는 모른다고 안내 | 지식베이스에 없는 질문에 환각 없이 답하기, 전문 상담 대체 불가 고지 |
| `edge` (25%) | 애매·복잡·희귀 케이스 | 다자녀 요청(범위 밖), 재고 없음, 요청 재료 전부 알레르기 충돌, 만료 재료 |
| `guardrail` (15%) | 정책 위반 유도·PII·프롬프트 인젝션 | 시스템 프롬프트 탈취 시도, "알레르기 검사 생략해줘", 다른 가정 데이터 요청 |

**`expected_traits`/`forbidden`은 텍스트 채점이지 식단 데이터 채점이 아니다.** "달걀 알레르기가 있어서 제외했어요"처럼 재료명이 답변에 **등장하되 제외 사유로 언급되는 경우**를 오답으로 잡지 않도록, 자동 채점 시 `forbidden`은 "~가 들어간 메뉴를 추천함" 같은 결과 단정 표현으로 적고, 실제 안전성 판정(추천된 레시피의 성분 전개)은 6.2의 보조 검사로 별도 수행한다.

### 6.2 이 스키마로 표현 못 하는 것 — 보조 검사

7컬럼·단발 질의 스키마는 answer 품질 평가에는 맞지만, 아래 두 가지는 **단발 질의로 증명이 안 된다.** `test_queries.csv`와는 별도로 평가 스크립트가 추가로 수행한다.

1. **HITL의 승인 후 실제 실행** — id=6은 "승인을 요청하는지"까지만 본다. 승인(`y`) 이후 도구가 실제로 실행되는지는 `thread_id` 고정값으로 대화를 이어 붙인 2턴짜리 스모크 테스트를 코드로 따로 둔다.
2. **장기 기억의 세션 간 지속성** — id=4, 7, 17은 "저장된 값을 읽어 쓰는지"까지만 본다. 프로세스를 재시작해도 값이 남아있는지는 CSV 채점이 아니라 통합 테스트(`update` 호출 → 프로세스 재기동 → `get` 호출 → 값 비교)로 검증한다.
3. **레시피 성분 전개 기반 안전성 자동 채점** — `check_allergen`이 실제로 호출됐는지는 `expected_tools`로 보지만, 최종 추천에 알레르기 성분이 물리적으로 없는지는 답변 텍스트가 아니라 `trace`에 담긴 추천 레시피 ID를 `allergen_map.json` + `allergy_profile.json`으로 재계산해 검증한다.

**전 케이스 공통 검사** (CSV에 컬럼을 두지 않고 러너가 항상 확인):
- 모든 답변에 전문 상담 고지 문구가 포함되는가 (정책 4)
- `expected_tools`에 없는 위험 도구(`save_meal_plan_to_file`, `override_preference_constraint`)가 승인 없이 실행되지 않았는가

---

## 7. 데이터 스키마

실제 파일: `data/recipes.json`, `data/allergen_map.json`, `data/allergy_profile.json`, `data/memory_store.json`, `data/knowledge/*.md`
모든 파일은 최상단에 `schema_version`을 둔다. 스키마가 바뀌면 값을 올리고 로더에서 확인한다.

### 7.1 공통 설계 결정

**(1) 재료는 자유 문자열이 아니라 정규화 ID로 참조한다**
레시피에 "진간장 1큰술"처럼 적어두면 알레르겐 판정이 문자열 매칭에 의존하게 되고, 표기가 하나만 달라도 안전 검사가 **조용히** 뚫린다. 레시피는 `ingredient_id`만 참조하고, 표시명·분량은 별도 필드에 둔다. 사용자가 말하는 다양한 표현("계란", "난류")은 `allergen_map`의 `aliases`가 흡수한다.

**(2) 매핑에 없는 재료는 "안전"이 아니라 "미상"이다 (fail-closed)**
`allergen_map.ingredients`에 없는 `ingredient_id`가 레시피에 등장하면 안전하다고 보지 않고 **로딩 단계에서 에러를 낸다.** "알레르기 충돌 0건" 기준은 *검사를 통과했다*가 아니라 *모든 재료가 검사 대상이었다*가 전제다. 이 검증이 없으면 재료 하나 빠뜨린 것이 곧 안전 사고가 된다.

**(3) 나이는 전부 개월 수(`_months`)로 통일한다**
"5살"과 "62개월"을 섞으면 경계 계산에서 반드시 사고가 난다. 사용자 입력이 "5살"이어도 저장·비교는 개월로 한다. 레시피의 `max_age_months: null`은 상한 없음을 뜻한다.

**(4) 알레르겐은 규제 목록이 아니라 사용자가 정의한다**
"무엇이 알레르기 유발물질인가"는 일반 목록으로 정하지 않는다. **우리 아이가 실제로 무엇에 반응하는가**만이 판정 기준이므로, 사용자가 `data/allergy_profile.json`에 직접 적고 그 파일이 **유일한 진실 원천**이 된다.

이 결정의 실질적 효과:
- 규제 표시 대상이 아닌 것도 걸러진다 (멸치·참깨처럼 표시 대상 19종에 없는 항목)
- 규제 목록에 있어도 우리 아이와 무관하면 **거르지 않는다** — 불필요하게 후보가 줄지 않는다
- 식약처 고시 개정을 추적할 필요가 없다

역할이 이렇게 갈린다:

| 파일 | 답하는 질문 | 누가 쓰나 |
|---|---|---|
| `allergen_map.json` | "이 재료에 **무엇이 들어 있나**" (간장 → 대두·밀) | 서비스가 제공 |
| `allergy_profile.json` | "우리 아이가 **무엇을 피해야 하나**" | **사용자가 작성** |

**(5) 사용자 표현은 성분(component)으로 해석한다**
사용자는 "계란", "난류", "생선"처럼 제각각 적는다. `allergen_map`의 `components[].aliases`가 표현을 정규화하고, `component_groups`가 넓은 말을 펼친다 (생선 → 고등어·멸치). 사용자가 특정 재료를 콕 집어 빼고 싶으면 `also_avoid_ingredients`에 재료 ID를 직접 넣는다.

**(6) 해석 결과를 반드시 사용자에게 보여준다 (silent no-match 방지)**
사용자가 적은 말이 **아무 재료에도 걸리지 않으면 가장 위험하다.** 오타("계랸")나 사전에 없는 표현("조개")을 적었는데 조용히 통과하면, 필터가 걸린 줄 알고 안심한 채 위험한 메뉴를 받는다.
→ 로딩 시 각 `term`이 어떤 성분·재료로 해석됐는지 출력하고, **해석 결과가 비면 에러로 기동을 막는다.** 이것이 사용자 정의 방식에서의 fail-closed다.

**(7) 알레르기는 대화로 수정하지 않는다**
`update_household_memory`는 나이·선호·냉장고 재료만 건드리고, `allergy_profile.json`은 **읽기 전용**으로만 접근한다. 에이전트가 대화를 오인식해 알레르기 항목을 지우는 것이 이 서비스에서 가능한 최악의 사고다. 알레르기 변경은 사용자가 파일을 직접 고친다.

**(5) 숨은 알레르겐은 `hidden: true`로 표시한다**
간장→밀, 마요네즈→알류처럼 이름만 봐서는 모르는 항목에 플래그를 단다. 답변에서 "간장에 밀이 들어 있어 제외했어요"처럼 **왜 걸렀는지 설명**할 때 이 플래그를 쓴다.

### 7.2 `recipes.json`

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | 고유 키 (snake_case) |
| `name` | string | 메뉴명 (표시용) |
| `meal_types` | enum[] | `breakfast` / `lunch` / `dinner` / `snack` |
| `min_age_months` | int | 권장 최소 개월 |
| `max_age_months` | int \| null | 권장 최대 개월. `null`은 상한 없음 |
| `texture` | enum | `puree` / `mashed` / `chopped` / `regular` — 질식 위험과 직결되므로 나이와 **별도로** 둔다 |
| `base_ingredient` | string | 대표 재료 ID. v1은 쓰지 않지만 다자녀 확장의 조리법 매칭 키라 미리 채워둔다 |
| `ingredients[]` | object[] | `{ ingredient_id, amount, optional }` |
| `nutrition_tags` | string[] | 영양소 포커스 검색용 |
| `cook_minutes` | int | 조리 시간 |
| `difficulty` | enum | `easy` / `normal` / `hard` |
| `steps` | string[] | 3줄 내외 요약. 실용성 기준("장 보고 요리할 수 있겠다")을 위해 필수 |
| `source` | string | 출처 |

`optional: true`인 재료는 **알레르기 충돌 시 빼고 만들 수 있다.** 이 경우 메뉴 전체를 버리지 않고 "달걀을 빼고 만드세요"로 안내할 수 있어 후보 고갈을 줄인다. 단 `optional: false` 재료가 충돌하면 메뉴를 제외한다.

### 7.3 `allergen_map.json` (재료 구성 사전 — 서비스 제공)

"무엇이 알레르기인가"는 여기서 정하지 않는다. **어떤 재료에 무엇이 들어 있는가**만 기술한다.

| 필드 | 타입 | 설명 |
|---|---|---|
| `components.<name>.aliases` | string[] | 그 성분을 가리키는 사용자 표현들 ("달걀"/"계란"/"난류") |
| `component_groups.<name>` | string[] | 넓은 말을 성분들로 펼치는 묶음 (생선 → 고등어·멸치) |
| `ingredients.<id>.display` | string | 표시명 |
| `ingredients.<id>.components` | string[] | 이 재료에 든 성분. `components` 키에 있는 값만 허용 |
| `ingredients.<id>.aliases` | string[] | 재료를 가리키는 사용자 표현 (기억 갱신·질문 파싱용) |
| `ingredients.<id>.hidden` | bool | 이름만 봐선 모르는 성분이 든 재료 (간장→밀) |
| `ingredients.<id>.note` | string | 판단 근거 메모 |

`hidden: true`는 답변에서 **"간장에 밀이 들어 있어 제외했어요"처럼 왜 걸렀는지 설명**할 때 쓴다.

### 7.4 `allergy_profile.json` (사용자 작성 — 판정의 유일한 원천)

| 필드 | 타입 | 설명 |
|---|---|---|
| `children[].name` | string | `memory_store`의 자녀와 이름으로 연결한다 |
| `children[].allergies[].term` | string | **사용자가 평소 쓰는 말 그대로.** 성분명("계란")도, 넓은 말("생선")도 가능 |
| `children[].allergies[].severity` | enum | `severe` / `moderate` / `mild` — 답변 톤과 경고 강도를 정한다 |
| `children[].allergies[].note` | string | 증상·진단 메모. 판정에는 쓰지 않고 사용자 참고용 |
| `children[].allergies[].also_avoid_ingredients` | string[] | `term`으로 안 잡히는 재료를 **직접** 지정하는 탈출구 |

**해석(resolve) 순서** — `term`을 아래 순으로 맞춰보고, 전부 실패하면 에러:
1. `component_groups`의 키와 일치 → 그 묶음의 성분 전체
2. `components.<name>.aliases`와 일치 → 그 성분
3. `ingredients.<id>.aliases`와 일치 → 그 재료 한정
4. 실패 → **기동 중단.** 오타나 사전에 없는 표현을 조용히 넘기지 않는다

`severity`는 **필터링 강도를 바꾸지 않는다.** `mild`여도 똑같이 제외한다. 회피 수준을 낮추는 판단은 보호자와 의사의 몫이지 에이전트의 몫이 아니다.

### 7.5 `memory_store.json` (장기 기억)

| 필드 | 타입 | 설명 |
|---|---|---|
| `household_id` | string | v1은 `"family_001"` 고정 |
| `children[]` | object[] | `{ name, age_months, birth_date, dislikes, updated_at }` — **알레르기 필드는 없다** (7.1-(7)) |
| `children[].dislikes` | string[] | 싫어하는 재료. 편식은 `override_preference_constraint`로 해제 가능하지만 알레르기는 불가하므로 저장 위치 자체를 분리했다 |
| `ingredients[]` | object[] | `{ ingredient_id, display, quantity, expiry_date, updated_at }` |
| `ingredients[].expiry_date` | date \| null | 쌀·소금처럼 기한이 무의미한 항목은 `null` |
| `ingredients[].quantity` | string | v1은 "1모", "2개" 같은 자유 문자열. 수량 연산이 필요해지면 `{value, unit}`으로 확장 |
| `change_log[]` | object[] | `{ at, action, target, detail }` — 정책 7(명시적 갱신)이 지켜졌는지 감사용 |

`birth_date`가 있으면 `age_months`를 계산해 덮어쓴다. 없으면 사용자가 말한 시점의 `age_months`를 그대로 쓰되, 시간이 지나면 낡는다는 점을 감안해 `updated_at`을 함께 본다.

### 7.6 `data/knowledge/*.md` (RAG 문서)

문서마다 YAML frontmatter를 둔다. `id`는 응답의 `contexts`에서 출처를 가리키는 키가 된다.

```yaml
---
id: iron_intake
title: 영유아 철분 섭취 가이드
topics: [철분, 빈혈, 이유식]
age_range_months: [6, 36]
source: 출처 표기
updated_at: 2026-09-16
---
```

**한국어 TF-IDF 주의점**: 공백 단위로 토큰화하면 조사 때문에 "철분이"와 "철분을"이 다른 토큰이 되어 검색이 거의 동작하지 않는다. 형태소 분석기를 붙이거나, 최소한 **문자 2~3-gram**(`analyzer="char_wb"`)으로 벡터화한다. 청크는 문서를 heading 단위로 자르고 `id`와 heading을 함께 보관해 `contexts`에 출처를 실을 수 있게 한다.

### 7.7 로딩 시 검증 규칙

아래를 통과하지 못하면 **에이전트를 기동하지 않는다.** 안전 판정의 전제 조건이므로 경고로 넘기지 않는다.

1. 모든 `recipes[].ingredients[].ingredient_id`가 `allergen_map.ingredients`에 존재한다
2. 모든 `ingredients.<id>.components` 값이 `allergen_map.components` 키에 존재한다
3. 모든 `component_groups`의 원소가 `components` 키에 존재한다
4. **모든 `allergy_profile`의 `term`이 하나 이상의 성분 또는 재료로 해석된다** — 해석 결과가 비면 에러 (7.1-(6))
5. 모든 `also_avoid_ingredients`가 `allergen_map.ingredients`에 존재한다
6. `allergy_profile.children[].name`이 `memory_store.children[].name`과 매칭된다
7. `recipes[].id`와 `allergen_map` 키에 중복이 없다
8. `min_age_months <= max_age_months` (max가 null이 아닐 때)
9. `meal_types` / `texture` / `difficulty`가 `enums`에 정의된 값이다

기동 시 아래를 콘솔에 출력해 사용자가 눈으로 확인하게 한다.

```
[알레르기 프로필] 첫째
  "계란"  → 성분 달걀      → 제외 재료: 달걀, 마요네즈, 식빵
  "우유"  → 성분 우유      → 제외 재료: 우유, 치즈, 버터, 플레인 요거트, 식빵
  "멸치"  → 성분 멸치      → 제외 재료: 멸치육수
  → 총 제외 재료 8종 (식빵은 달걀·우유 양쪽에 걸림)
```
